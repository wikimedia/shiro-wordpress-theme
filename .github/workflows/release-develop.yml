name: Develop Release

on:
  push:
    branches:
      - develop

permissions:
  contents: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref_name }}
  cancel-in-progress: true

jobs:
  release:
    name: "Update release-develop branch"
    uses: humanmade/hm-github-actions/.github/workflows/build-and-release-node.yml@70b862a2bc0669a2123d5bf3d909642c8dc07483 # v0.5.0
    with:
      node_version: 22
      source_branch: develop
      release_branch: release-develop
      built_asset_paths: assets/dist
      build_script: |
        npm ci
        npm run build
